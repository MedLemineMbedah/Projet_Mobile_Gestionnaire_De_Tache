import 'package:flutter/material.dart';

class ResourceController extends StatelessWidget {
  const ResourceController({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}